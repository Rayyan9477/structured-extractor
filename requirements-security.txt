# Security-focused dependencies for production deployment
# Add these to your main requirements.txt for production

# File type detection (prevents malicious file uploads)
python-magic>=0.4.27

# Safe XML parsing (prevents XXE attacks)
defusedxml>=0.7.1

# Cryptographic operations
cryptography>=41.0.0

# Input validation and sanitization
validators>=0.20.0

# Secure password hashing (if authentication is added)
bcrypt>=4.0.1

# JWT handling (if authentication is added)
PyJWT>=2.8.0

# Rate limiting (if web deployment)
slowapi>=0.1.9

# Security headers (if web deployment)
secure>=0.3.0

# Environment variable validation
python-dotenv>=1.0.0